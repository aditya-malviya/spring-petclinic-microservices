pipeline {
   agent any
   stages {
      stage('Hello') {
         steps {
          sh label: '', 
           script: 
           '''
           cd spring-petclinic-config-server
           mvn clean package
           '''
         } 
      }
   }
}
